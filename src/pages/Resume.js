import Container from 'react-bootstrap/Container';

function Resume() {
    return (
        <Container>
            <h1>This is the resume page.</h1>
        </Container>
    );
}

export default Resume;